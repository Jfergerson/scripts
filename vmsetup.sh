#!/usr/bin/env bash
sudo apt-get install sysstat
sudo apt-get install rt-tests
sudo apt-get install vim
sudo apt-get install ruby
git config --global user.email = "testing_vm@garbo.com"
git config --global user.name = "john"
